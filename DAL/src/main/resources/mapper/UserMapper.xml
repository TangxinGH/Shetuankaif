<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.UserMapper">

    <!-- 查询所有 -->
    <select id="findAll" resultType="entity.User">
        SELECT * FROM user
    </select>

    <!-- 根据ID查询 -->
  <!--  <select id="findById" resultType="entity.User" parameterType="Integer">
        SELECT * FROM user WHERE id = #{id}
    </select>-->
    <!--登录-->
    <select id="login" resultType="entity.User" >
        SELECT * FROM user WHERE Sno = #{param1} and Password = #{param2}
    </select>

    <!--注册-->
    <select id="register" resultType="entity.User" parameterType="entity.User">
        insert into user (Sno,Sname,SCollege,Password,Sex,Age) values ( #{Sno} ,#{Sname}, #{SCollege}, #{Password}, #{Sex}, #{Age})
    </select>

  <!--   创建
    <insert id="create" parameterType="entity.User">
        INSERT INTO user(username, password) VALUES (#{username}, #{password})
    </insert>

     删除
    <delete id="delete" parameterType="Long">
        DELETE FROM user WHERE id = #{id}
    </delete>-->

    <!-- 更新
    <update id="update" parameterType="entity.User">
        UPDATE user SET WHERE 1=1
        <if test="sno!= null">
           sno = #{sno},
        </if>
        <if test="sname!= null">
            sname = #{sname}
        </if>
        WHERE id = #{id}
    </update>-->

    <!-- 根据用户名查询 -->
    <select id="findById" parameterType="Integer" resultType="entity.User">
        SELECT * FROM user WHERE Sno = #{Sno}
    </select>
</mapper>
