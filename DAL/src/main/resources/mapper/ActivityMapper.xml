<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.ActivityMapper">
    <!--以最新的为准-->
    <!-- 查询所有 -->
    <select id="getAllActivities" resultType="entity.Activity">
        SELECT * FROM activity
    </select>

    <!-- 根据标题查询 -->
    <select id="getActivityByActivityTitle" resultType="entity.Activity" parameterType="String">
        SELECT * FROM activity Act WHERE 1=1
        <if test="Act_Title != null">
            and Act.Act_Title = #{Act_Title,jdbcType = VARCHAR}
        </if>
    </select>

    <!--查询所有标题-->
    <select id = "getAllActivityTitles" resultType="String">
        SELECT Act_Title FROM activity
    </select>

    <!-- 添加活动 -->
    <insert id="addAnActivity" parameterType="entity.Activity">
        INSERT INTO activity(Act_Title, Act_Content, Act_Publish_Time, Act_Author, Act_Author_ID, Act_Attachment)
        VALUES (#{Act_Title,jdbcType = VARCHAR},
            #{Act_Content,jdbcType = CLOB},
            #{Act_Publish_Time,jdbcType = TIMESTAMP},
            #{Act_Author,jdbcType = VARCHAR},
            #{Act_Author_ID,jdbcType = INTEGER},
            #{Act_Attachment,jdbcType = CLOB}
         )
    </insert>

    <!-- 删除活动 -->
    <delete id="deleteAnActivityByTitle" parameterType="String">
        DELETE FROM activity WHERE 1 = 0
        <if test="Act_Title != null">
            or Act_Title = #{Act_Title,jdbcType = VARCHAR}
        </if>
    </delete>

    <!-- 更新活动 -->
    <update id="updateAnActivityByTitle" parameterType="entity.Activity">
        UPDATE activity
        <trim prefix = "set" suffixOverrides=",">
            <if test = "Act_Title != null">Act_Title = #{Act_Title,jdbcType = VARCHAR},</if>
            <if test = "Act_Content != null">Act_Content = #{Act_Content,jdbcType = CLOB},</if>
            <if test = "Act_Publish_Time != null">Act_Publish_Time = #{Act_Publish_Time,jdbcType = TIMESTAMP},</if>
            <if test = "Act_Attachment != null">Act_Attachment = #{Act_Attachment,jdbcType = CLOB}</if>
        </trim>
        WHERE 1 = 0
        <if test="Act_Title != null">
            or Act_Title = #{Act_Title,jdbcType = VARCHAR}
        </if>
    </update>

</mapper>
