<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="mapper.UserMapper">

    <!-- 查询所有 -->
    <select id="findAll" resultType="entity.User">
        SELECT * FROM user
    </select>

    <!-- 根据ID查询 -->
    <select id="findById" resultType="entity.User" parameterType="Integer">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <!--登录-->
    <select id="login" resultType="entity.User" parameterType="String">
        SELECT * FROM user WHERE sno = #{param1} and sname = #{param2}
    </select>

    <!--登录-->
    <select id="register" resultType="entity.User" parameterType="User">
        insert into user (sname,sname) values ( #{username} , #{password})
    </select>

    <!-- 创建 -->
    <insert id="create" parameterType="entity.User">
        INSERT INTO user(username, password) VALUES (#{username}, #{password})
    </insert>

    <!-- 删除 -->
    <delete id="delete" parameterType="Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <!-- 更新 -->
    <update id="update" parameterType="entity.User">
        UPDATE user SET WHERE 1=1
        <if test="username != null">
            username = #{username},
        </if>
        <if test="password != null">
            password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <!-- 根据用户名查询 -->
    <select id="findByName" parameterType="String" resultType="entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
</mapper>
